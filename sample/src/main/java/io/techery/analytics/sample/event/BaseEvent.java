package io.techery.analytics.sample.event;

public interface BaseEvent {
   // it is handy to have all tests implement some interface to create
   // one singleton janet pipe for all analytic events
}
